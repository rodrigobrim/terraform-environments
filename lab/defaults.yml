---

tfvar:
  region: us-west1
  credentials: /keys/gcp-admin.json
  project: brim-{{ app }}-{{ env }}
  ip_cidr_range: 10.10.0.0/0
  bucket: brim-terraform-state-files


repositories:
  - name: vpc
    provider: github.com
    user: rodrigobrim
    repo: terraform-lab-vpc
  - name: gke
    provider: github.com
    user: rodrigobrim
    repo: terraform-lab-gke
